# Development dependencies input file for pip-compile
# Include production requirements
-r requirements.in

# AWS SDK - included here for local development/testing
# (boto3 is pre-installed in Lambda runtime, so not needed in production package)
boto3>=1.42.0

# Testing
pytest>=9.0.0
pytest-cov>=7.0.0
pytest-mock>=3.15.0

# Type checking
mypy>=1.19.0
boto3-stubs[secretsmanager]>=1.42.0

# Linting and formatting
ruff>=0.14.0
